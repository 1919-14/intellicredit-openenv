# training/requirements.txt
# Separate from main requirements.txt to keep Docker image lean.
# Only needed for local PPO training, NOT for HuggingFace Spaces deployment.
stable-baselines3>=2.1.0
tensorboard>=2.14.0
gymnasium>=0.29.0
matplotlib>=3.7.0
numpy>=1.24.0
